apiVersion: apps/v1
kind: Deployment
metadata:
  name: scp
  namespace: scp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scp
  template:
    metadata:
      labels:
        app: scp
    spec:
      containers:
        - name: scp
          image: abdysamat/spring-certified-professional:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
